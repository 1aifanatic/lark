---
name: Feature request
about: Suggest something LARK should do
labels: enhancement
---

**What you are trying to do**
The task, not the solution — it often has a simpler answer than expected.

**What you do today instead**

**Why LARK is the right place for it**

Worth knowing: LARK pastes into the AI platforms' own web UIs rather than calling their
APIs, so it can never read the reply. Anything that needs the model's output back
(summaries in the panel, chaining, saved answers) is out of scope by design.
